file_format: '0.3'

propagator:
  composite: []

tracer_provider:
  processors:
  - simple:
      exporter:
        console:
  sampler:
    always_on:

instrumentation:
  general:
  php:
    example_instrumentation:
      span_name: ${EXAMPLE_INSTRUMENTATION_SPAN_NAME:-example span}
