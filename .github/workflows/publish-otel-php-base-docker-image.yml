name: publish-otel-php-base-docker-image
on:
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:
  push:
    paths:
      - docker/Dockerfile
      - .github/workflows/publish-otel-php-base-docker-image.yml
  pull_request:
    paths:
      - docker/Dockerfile
      - .github/workflows/publish-otel-php-base-docker-image.yml
jobs:
  push_to_registry:
    name: OpenTelemetry PHP base docker image creation
    strategy:
      matrix:
        php-version: ['8.0', '8.1', '8.2', '8.3', '8.4']
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    timeout-minutes: 500
    steps:

      - name: check out the repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@b5ca514318bd6ebac0fb2aedd5d36ec1b5c232a2 # v3 on 2025-04-02, TODO: consider using a release

      - name: Login to GitHub Container Registry
        uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772 # v3 on 2025-04-02, TODO: consider using a release
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.php-version }} to ghcr.io
        uses: docker/build-push-action@471d1dc4e07e5cdedd4c2171150001c434f0b7a4 # v6 on 2025-04-02, TODO: consider using a release
        if: github.ref != 'refs/heads/main'
        with:
          push: false
          file: docker/Dockerfile
          build-args: PHP_VERSION=${{ matrix.php-version }}
          platforms: linux/amd64,linux/arm/v8,linux/arm64
          tags: ghcr.io/open-telemetry/opentelemetry-php/opentelemetry-php-base:${{ matrix.php-version }}

      - name: Build and push ${{ matrix.php-version }} to ghcr.io
        uses: docker/build-push-action@471d1dc4e07e5cdedd4c2171150001c434f0b7a4 # v6 on 2025-04-02, TODO: consider using a release
        if: github.ref == 'refs/heads/main'
        with:
          push: true
          file: docker/Dockerfile
          build-args: PHP_VERSION=${{ matrix.php-version }}
          platforms: linux/amd64,linux/arm/v8,linux/arm64
          tags: ghcr.io/open-telemetry/opentelemetry-php/opentelemetry-php-base:${{ matrix.php-version }}
