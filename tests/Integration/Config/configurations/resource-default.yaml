file_format: '1.0-rc.2'

resource:
  schema_url: https://opentelemetry.io/schemas/1.38.0
  detection/development:
    detectors:

tracer_provider:
  processors:

meter_provider:
  readers:

logger_provider:
  processors:
