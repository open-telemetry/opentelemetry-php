file_format: "1.1"
logger_provider:
  limits:
    attribute_count_limit: 2
    attribute_value_length_limit: 5
  processors:
    - batch:
        exporter:
          otlp_file/development:
            output_stream: stdout
